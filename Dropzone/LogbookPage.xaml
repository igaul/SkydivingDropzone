<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Dropzone.LogbookPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add Jump"
                     Clicked="ToolbarItem_Clicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Logbook"
                VerticalOptions="CenterAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>

    <ListView x:Name="jumpsListView">
        <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Date}" />
                <!--
                <TextCell Text="{Binding JumpNumber}" />
                <TextCell Text="{Binding Place}" />
                <TextCell Text="{Binding Aircraft}" />
                <TextCell Text="{Binding Equipment}" />
                <TextCell Text="{Binding FreeFallTime}" />
                <TextCell Text="{Binding FlyingType}" />
                -->

            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
    
</ContentPage>